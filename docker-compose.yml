services:
  app:
    build: .
    ports:
      - "41316:3000"
    environment:
      - DATABASE_URL=file:/app/data/redmine.db
    volumes:
      - redmine-data:/app/data
    restart: unless-stopped

volumes:
  redmine-data:
